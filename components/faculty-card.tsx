interface FacultyCardProps {
  id: string;
  title: string;
  imageUrl: string;
  description: string;
  school: string;
}

export const FacultyCard = ({
  // id,
  // title,
  // imageUrl,
  // description,
  // school,
}: FacultyCardProps) => {
  return <div>Faculty Card</div>;
};
